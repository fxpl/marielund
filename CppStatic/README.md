= Before building tests (or doing 'make all') =
Check out googletest (including googlemock) and put it in a directory named external:
	mkdir external
	cd external
	git clone https://github.com/google/googletest.git

